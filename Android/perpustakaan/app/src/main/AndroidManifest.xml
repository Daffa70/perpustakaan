<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uas.perpustakaan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.BookingDetailActivity"></activity>
        <activity android:name=".activity.BookingActivity" />
        <activity android:name=".activity.EditBookActivity" />
        <activity android:name=".activity.BookDetailActivity" />
        <activity android:name=".CekSessionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivityAdmin" />
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.RegistrasiActivity" />
        <activity android:name=".activity.LoginAdminActivity" />
        <activity android:name=".activity.LoginUserActivity" />
        <activity android:name=".activity.MainMenuActivity" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>